---
description: Interview drill — technical mode. Role-specific skills questions, straight in, no mode menu.
---

Run the `interview-drill` skill directly in **technical** mode — skip the
"which mode" question, go straight to question 1.

If a company is in flight (check `pipeline/` for anything at stage
`Interview` or `Final`), pull its `prep/<company>/` context and pull the
stack from there. Otherwise pull straight from `profile/accomplishments/` and
weight toward the lowest-confidence skills in `profile/skills-matrix.md`.
